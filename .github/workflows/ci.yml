name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          # Only main writes the cache; PR/feature branches read it. Stops every branch
          # from saving its own near-duplicate entry and evicting main's from the 10 GB budget.
          cache-read-only: ${{ github.ref != 'refs/heads/main' }}
          # Before saving, drop anything restored but unused this run (stale deps, old Gradle
          # dists, evicted build-cache entries) so the Gradle home cache stays bounded over time.
          cache-cleanup: on-success

      # Sideloads the Sentry source-context/mapping upload (needs a secret token we don't
      # set in CI); -PsentryUpload=false makes those steps no-op so the build needs no secrets.
      - name: Tests, lint, and debug build
        run: ./gradlew test lintDebug assembleDebug -PsentryUpload=false

      - name: Upload reports
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: |
            android/app/build/reports/tests/
            android/app/build/reports/lint-results-*.html
          if-no-files-found: ignore
